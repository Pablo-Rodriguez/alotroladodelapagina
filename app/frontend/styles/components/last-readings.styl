@import 'nib'

:host
    display block
    color var(--dark-color)
    #wrapper
        background-color var(--light-color)
        border-radius 5px
        padding 0 1em
        padding-bottom 1em
        h3
            text-align center
            font-size 24px
            padding 1em 0
