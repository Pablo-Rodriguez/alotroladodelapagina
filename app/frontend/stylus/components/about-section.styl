@import 'nib'

:host
    display block
    padding 1em
    section
        display block
        background-color #fff
        box-shadow 0 0 5px rgba(0, 0, 0, .3)
        border 2px solid var(--dark-color)
        border-radius 5px
        margin 1em auto
        max-width 1000px
